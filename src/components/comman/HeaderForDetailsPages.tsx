import React from 'react';
import { Box, Flex } from '@mantine/core';

// interface HeaderForDetailsPagesType {
//   movieId: number;
// }
const HeaderForDetailsPages: React.FC = () => {
  //   const { data } = useGetMoviesDetailsQuery(movieId);
  return (
    <>
      <Box>
        <Flex></Flex>
      </Box>
    </>
  );
};

export default HeaderForDetailsPages;
